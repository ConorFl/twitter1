$(document).ready(function() {
  // var container = $('.container');
  // if (container.val().length == 0) {
  //   container.hide();
  // }
  // $(document).on('click', 'input[name="show_tweets"]', function() {
  //   container.show();
  // })
});
